---
description: 对人有帮助的日志默认可索引；项目留档日志 Visibility:project → noindex,nofollow；详述过程
alwaysApply: true
---

# 开发日志（dev-logs）

每次有**需要记录**的 Q&A 结束后，在 **`dev-logs/YYYY-MM/`** 新建一篇英文日志。  
对人有帮助的篇：详细写清解决过程与方案，**默认可索引**并进入 sitemap。  
仅项目留档、对人无帮助的篇：仍须落盘，但头部标 `Visibility: project` → 构建为 **`noindex, nofollow`**，且**不进** sitemap。

公开页由 `npm run build:logs` 生成到 `public/devlogs/`（`robots.txt` **允许** `/devlogs/`；对人有帮助的篇可索引；索引列表仍可浏览全部篇目）。

## 1. 何时落盘与可见性（每轮必做）

| 类型 | 何时写 | Markdown 头部 | 公开 SEO |
| --- | --- | --- | --- |
| **People**（默认） | 对后人/站内读者有帮助 | 省略，或 `Visibility: people` | 可索引；进 sitemap；详述过程与方案 |
| **Project** | 项目需要留档，但对外无帮助（内部状态、无增量审计痕迹等） | **必须** `Visibility: project`（或 `Robots: noindex, nofollow`） | `<meta name="robots" content="noindex, nofollow">`；**不进** sitemap；站内清单仍可见 |

对人有帮助的信号（→ People）：可复用决策/规则、实质交付、可学的排查/howto、读者不看聊天也能复用的信息增量。

**仍跳过落盘**（聊天照常回答）：

- **仅** `git add` / `git commit` / `git push`（且无其它可记交付）
- 纯确认 / 闲聊 / 无实质内容的半截对话
- 仅创建/更新本日志文件本身
- 密钥、凭证、`.env`、个人隐私（日志中**不得**写入）
- 连项目留档都不值得的空壳「已完成」清单（无任何可复述事实）

同一主题多轮：优先**一篇 consolidated**；不要把有帮助的问答拆成多篇空壳。

## 2. 正文深度

**People 篇** `[try to solve]` 必须详细（禁止只写一句结论），至少覆盖：

1. **Context** — 问题/目标
2. **Process** — 排查或实现步骤
3. **Root cause / analysis** — 根因或取舍
4. **Solution** — 最终方案与验证
5. **Notes / boundaries** — 边界与相关路径

**凡本轮改动了工具页**（catalog / i18n 分片 / `src/pages/*Page.ts` / 预渲染工具 HTML）：在 **`[question]` 之后、`[try to solve]` 之前**增加 **Tool links (English)** 小节，列出**英文默认语**可点开的生产 URL（默认语无语言前缀）：

```markdown
[question]
<user's original question; may keep original language>

## Tool links (English)
- https://onlinefreetools.org/tools/<slug>
- https://onlinefreetools.org/tools/<slug-2>

[try to solve]
```

- 只列本轮实际改动或需验收的 slug；多工具按 slug 字母序或交付优先级。
- 使用 apex 生产域 `https://onlinefreetools.org`；**不要**写 `/en/tools/...`（英文默认路径无 `/en`）。
- 若同时需要中文验收链接，可另起一行 `Chinese: https://onlinefreetools.org/zh/tools/<slug>`，但 **English 链接为必填**。
- 纯运维/规则/无工具页改动的日志：省略本小节。

**Project 篇** 可较短，但仍须写清「记了什么、为何需要留档」，禁止无事实空壳。涉及工具改动时同样附上 **Tool links (English)**（位置同上：紧跟 `[question]`）。

## 3. SEO 内容质量

权威：`.cursor/rules/seo-google-policy.mdc`。

- **People 篇**：按可索引、people-first 标准写；唯一价值；禁止换皮空壳与关键词堆砌。
- **Project 篇**：用 noindex/nofollow + 排除 sitemap 避免 scaled thin content 进索引；仍禁止密钥与虚假宣传。
- **语言**：新日志**全文英文**（`[question]` 可留原文）。
- **历史**：默认只追加。用户要求「梳理历史」时：空壳可迁 `_archive/`；对人无帮助但需留档的可改标 `Visibility: project` 后重建，而非一律删除。`_archive` / `_curation` 等 `_` 前缀目录不参与 `build:logs`。

## 4. 路径与文件名

- **目录**：`dev-logs/YYYY-MM/`
- **文件名**：`YYYY-MM-DD-HH-MM-<summary>.md`（本地 24h；kebab-case summary）

## 5. 正文模板（新日志）

```markdown
Date: <YYYY-MM-DD HH:MM>
Summary: <one English sentence: outcome or decision>
Visibility: people
# 项目留档且对人无帮助时改为：
# Visibility: project

[question]
<user's original question; may keep original language>

## Tool links (English)
- https://onlinefreetools.org/tools/<slug>
# （无工具页改动时可整节省略）

[try to solve]
## Context
...

## Process
...

## Root cause / analysis
...

## Solution
...

## Notes / boundaries
...

[actions]
- <optional: files or commands changed; never include creating this log file>
```

`Visibility` 可省略（默认 `people`）。等价写法：`Robots: noindex, nofollow`（构建同样按 project 处理）。别名：`internal` / `noindex` / `private` → project。

工具改动日志须含 **Tool links (English)**（见 §2）；格式为 `https://onlinefreetools.org/tools/{slug}`。

## 6. 范围与可见性

- **记**：People 详述；Project 留档并标 Visibility。
- **不记**：AI 末尾追问；创建日志文件本身；纯 git；密钥。
- **聊天**：完整展示；不因写日志缩短回复。
- **公开 URL**：`/devlogs/<文件名>.html`；Project 篇可访问但对爬虫 noindex,nofollow。

## 7. 构建

`npm run build:logs` / `build:site`：解析 `Visibility`/`Robots` → Project 篇注入 robots meta，并从 sitemap 排除该篇（索引页与 People 篇仍可进 sitemap）。

## 示例路径

`dev-logs/2026-09/2026-09-01-20-22-devlogs-helpful-default-detailed-process.md`
