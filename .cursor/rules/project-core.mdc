---
description: 仓库核心约定——权威序、命名、提交与安全；SEO 以 Google 官方为最高
alwaysApply: true
---

# 项目核心约定

- **平台**：Cloudflare Pages + Workers；本地/部署用 Wrangler。
- **结构**：根目录即项目根；对话日志在 `dev-logs/`（只追加，不改写历史）。
- **改动原则**：只改与请求直接相关的文件；不擅自引入框架或无关重构。
- **安全**：密钥/凭证走环境变量，不写入仓库。

## 命名与目录

- 文件/目录：`kebab-case`；环境名：`dev` / `staging` / `prod`。
- 站点源码：`src/`；静态输出：`public/`；i18n：`src/site/i18n/`；运维脚本与手册：`ops/`。
- **同域 vendor**：`public/vendor/`（bootstrap/fonts/jsquash/gifenc/wasm-feature-detect）须**入库**；Cloudflare 拉 GitHub 部署时无本地 `predeploy`。禁止再 gitignore 整树；改完跑 `npm run lint:vendor`（见 `cloudflare-workflow.mdc`）。
- 新工具立项讨论：`work-tasks/{slug}/`（含 `03-locale-briefs.md`；见 `work-tasks-tool-brief.mdc` / `tool-i18n-localization.mdc`）；**勾工程/页面/上线清单前**须完成 slug/title/关键词/desc 检索覆盖优化；实现仍走 `tool-creation.mdc`（catalog/i18n 分片经 `merge:tools` 生成；**交付前须 `npm run build:site`** 刷新 `public/_pages/*/index.html`，仅 merge 首页不会出现新工具）。
- 未来若拆 monorepo，可用 `apps/worker-*`、`apps/pages`；当前根目录布局即可。

## 提交信息

- 祈使句、简短主题；注明影响路径，例如 `worker: add hello route`。

## 权威序（冲突裁决）

**策略/合规冲突时按下列顺序**；下层不得覆盖上层：

1. **Google Search Central 现行官方文档**（镜像：`.cursor/rules/seo-google-policy.mdc`）
2. **运行代码**（`scripts/validate-tool-seo.mjs`、实际页面与 catalog）
3. **`.cursor/rules/*`**（Agent 可执行落地；须对齐 1）
4. **`docs/*`**（展开说明；须对齐 1–3；冲突时改 docs 或先改 rules 再同步）
5. **`.github/copilot-instructions.md`**（从 rules 单向同步摘要）

流程：策略变更 → 先更新对应 rule（及必要时 `seo-google-policy.mdc`）→ 再改 docs 对齐。  
**冲突须给出判断并经人工确认后再改文件。**

SEO 落地规则索引：`seo-google-policy` · `tool-i18n-seo` · `tool-i18n-localization` · `tool-creation` · `seo-html-defaults` · `work-tasks-tool-brief`。
