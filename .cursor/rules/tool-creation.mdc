---
description: 新建/修改工具页面的工程步骤、页面结构与 Information Gain 要求
globs: src/pages/**/*Page.ts,src/site/tool-catalog.d/**/*,src/site/i18n/tools/**/*,src/site/tool-catalog.json,src/site/toolPageRegistry.generated.ts,scripts/tool-modules/**/*,scripts/site/**/*,README.md
alwaysApply: false
---

# 工具创建规则

## 目标

工具页可交互、多语言、可检索、对用户有用（people-first），且相对 SERP 同类页有 **Information Gain**（见 `tool-i18n-seo.mdc` 与策略文档 §3）。

## 结构层隔离（必须）

每个工具的**可编辑真相源**按 slug 分片；合并产物由脚本生成，**禁止手改**。

| 角色 | 路径 |
|------|------|
| Catalog 分片 | `src/site/tool-catalog.d/{slug}.json`（含 `page.module` / `page.export` / `page.style`） |
| i18n 分片 | `src/site/i18n/tools/{slug}/{lang}.ts` |
| 站点共用文案 | `src/site/i18n/core/{lang}.ts`（非单工具改动勿动） |
| 页面实现 | `src/pages/{camel}Page.ts` |
| 立项 brief | `work-tasks/{slug}/` |
| **生成勿手改** | `src/site/tool-catalog.json`、`src/site/i18n/{lang}.ts`、`src/site/toolPageRegistry.generated.ts`、`scripts/site/i18n-tools.generated.mjs` |

流程：

1. 只改本工具分片 + 本工具 `*Page.ts` + icon + brief（及 README 清单条目）。
2. `npm run merge:tools`（合并 catalog / i18n / registry；**不够**刷新首页）。
3. **`npm run build:site`（硬性）**：在 `merge:tools` 之上重建 `public/_pages/*/index.html`、sitemap、vendor chrome。缺这一步则 `/`、`/zh/` 等静态首页**不会出现**新工具卡片（工具页路由可能已可用，首页仍是旧 HTML）。`start:dev` 默认会跑 `build:site`；若用了 `--no-build` 或只手动 `merge:tools`，交付前必须再跑一遍 `build:site`。
4. **不要**为新工具改 `src/index.ts`（由 `TOOL_PAGE_RENDERERS` 循环注册）。
5. 勿改其他工具的 `tool-catalog.d/`、`i18n/tools/`、`*Page.ts`。
6. 若必须改 related 反向链接或共用 chrome：显式 `CROSS_TOOL_UPDATE=1`，并在 PR 说明。
7. 隔离检查：`TOOL_SLUG={slug} npm run lint:tool-isolation`。

一次性拆分（仅迁移/重建分片时）：`npm run split:tools` → `npm run merge:tools`。日常开发：编辑分片 → `merge:tools` → **`build:site`**（或直接 `build:site`，其内含 merge）。

## 工程清单

> **生成本清单之前**：必须已完成「清单前检索覆盖优化」——对 **slug、title、关键词、desc** 分析用户搜索习惯与关键词覆盖并回写（覆盖表在 `02`/`notes`，含**主词→H1、次词→desc/FAQ/Use cases 逐词落点**；见 `tool-i18n-localization.mdc`「相关关键词落地」+ 步 0b）。未完成不得按下表开工。  
> **检索覆盖 Skill**：多工具 / 新工具实现时先读并执行 [`.cursor/skills/tool-coverage-pass/SKILL.md`](../skills/tool-coverage-pass/SKILL.md)；分阶段跑 `npm run coverage:gate`。

```
work-tasks/{slug}/              # 00–03 brief（含清单前覆盖表 + 03-locale-briefs）
npm run coverage:gate -- --slug={slug} --phase=0b   # 标 ready / 勾模块清单前
src/site/tool-catalog.d/{slug}.json   # slug / path / related ≥2 / FAQ / YMYL / localProcessing / page.*
src/pages/{camel}Page.ts
src/site/i18n/tools/{slug}/{lang}.ts  # 母版 → coverage:gate --phase=2；他语 → --phase=4
public/icons/tools/{slug}.svg   # 工具图标（注释优先 ASCII；见下方「图标 SVG」）
README.md                       # 「工具清单 / Tools List」中英条目（写入前须已过清单前覆盖）
npm run coverage:gate -- --slug={slug} --phase=all
npm run build:site              # 含 merge:tools；刷新首页 _pages + sitemap（勿只 merge）
npm run lint:seo
TOOL_SLUG={slug} npm run lint:tool-isolation
# 本地验收：带 Accept: text/html 打开 / 与 /zh/，确认新工具出现在 featured/all-tools
```

### Plan 多工具 todos（强制）

创建含 ≥2 个新工具的 Plan 时，**每个 slug** 须展开为：`brief-0b` → `i18n-master` → `cover-step2` → `i18n-locales` → `cover-step4` → `ship`（含 `build:site`）。禁止单一 todo「实现全部工具」。实现串行过 `coverage:gate`；可并行写各 slug 的 0b brief。

`src/index.ts` **无需**再 `registerToolPage`；首页卡片由 `build:site` 读合并后的 catalog 生成进 `public/_pages/*/index.html`，一般不必手改 `content-home.mjs`。

多语言：**按 locale brief 重写**（见 `tool-i18n-localization.mdc`），禁止批量 Translate 直出。  
文案顺序：① **清单前**优化 slug/title/关键词/desc → ② 写母版 title/description → ③ **生成后再**跑覆盖优化（步 2）→ ④ 逐语重写 → ⑤ 抽查语再优化（步 4）；勿首稿过 lint 即标完成。

## 图标 SVG（易踩坑）

- 浏览器按 **XML** 解析 `/icons/tools/*.svg`；注释里若混入 C0 控制字符（如 `0x07`/`0x18`/`0x1A`）会报 `xmlParseComment: invalid xmlChar`。
- **根因常见**：中文注释经部分写入/编码管道损坏。`image-exif` / `image-compress` / `image-optimizer` 已踩过。
- **约定**：工具图标注释用 **ASCII**（或干脆不写注释）；落盘后可用浏览器打开该 SVG URL 自检。
- `npm run lint:seo` 会跑 `validate-tool-icons`（非法控制符 → 失败）。

## 支持语言

以 `src/site/i18n/` 与 `src/site/i18n.ts` 的 `supportedLangs` 为准（当前 10 语：en, zh, es, ar, pt, id, fr, ja, ru, de）。新增语言需同步路由、hreflang、sitemap 与首页卡片。

## 首页、导航与 README

- 新工具必须在首页（featured / all-tools）和工具导航中有入口
- **凡新增/改 catalog 或首页文案后必须 `npm run build:site`**，刷新 `public/_pages/*/index.html` 与 sitemap；**禁止**以为 `merge:tools` 已更新首页（本地 `/zh/`「没有显示」新工具，多半缺此步）
- 仅当改了首页布局逻辑时才需动 `scripts/site/components/content-home.mjs`，改完同样要 `build:site`
- **同步更新根目录 `README.md` 的「工具清单 / Tools List」**：中英文各增一条（短标题 + 一句话说明 + 线上链接）；清单须与 `tool-catalog.json` 已上线工具一致，勿漏更

## 多语言链接

- 各语言 URL 可访问，例如 `/zh/tools/xxx`、`/tools/xxx`（en 默认无前缀）
- 页面展示目标语言全文，禁止占位符或错语言 fallback
- 每个工具页需完整 `hreflang` + `x-default`（见 `src/pages/site/layout.ts`）
- 文案本地化与 ≥3 轮核查见 `tool-i18n-localization.mdc`（勿英模直译后仅靠 lint）

## 页面结构（可见内容，自上而下）

与策略文档 §5.1 对齐：

```
H1 + 一句话摘要
【工具交互区】← 首屏（进页须自动跑样例并显示真实结果，见下节）
H2 How it works
H2 Formula / Rules / Algorithm（计算与转换类）
H2 Example（固定 Input→Output；须与默认样例行为一致）
H2 Use cases（2–3 条场景；长尾意图优先写此，慎拆 URL）
H2 FAQ（≥3）
H2 Related tools（catalog related ≥2）
H2 References（≥1 可点击权威链，YMYL 建议 ≥2）
Disclaimer（YMYL 必备）
```

**文案深度**（相对「三步怎么用」）：为何遇到该问题、输入类型差异、失败边界、参数选择、与竞品/其他方法差异。

**可见文案边界**：以上各区只写「用户完成任务需要知道什么」。禁止把 SEO/工程理由（doorway/空壳/薄页/slug/「双向放一页」「不拆 URL」自述）或实现库名当卖点写进正文；理由留 `work-tasks`/`docs`。见 `tool-i18n-localization.mdc` / `tool-i18n-seo.mdc`。

**Tier 2 / WASM**：首屏不拉大块 WASM；点击后懒加载（CWV）。

## 进页样例须实际跑出结果（硬性）

交互工具页除静态 H2 Example 文案外，**必须配置并可运行的样例**，且首屏进入后看到**真实处理结果**（对齐 `image-compress` / `image-crop`）：

1. **有 Load sample（或等价）按钮**：用合成/内置样例数据填充输入，并触发与主按钮相同的处理流水线。
2. **进页自动跑一遍**：脚本初始化结束后调用与 Load sample 相同的路径（`loadSample()` → 解码/计算 → 更新预览/结果区）；勿只塞占位文案或空态。
3. **结果区可见**：预览、数值、色板、转换输出等与用户手动操作后一致；失败时有明确错误，不得静默空结果。
4. **与 Example 文案一致**：可索引 Example 描述的 Input→Output 须与默认样例行为对得上（尺寸/色数/布局等默认值写进 `02-tool-info` 交互规格）。
5. **例外（须在 `02` 写明理由）**：依赖用户密钥/实时外网且无法安全造样例、或会误触发付费/破坏性 API 的工具——仍须提供「安全演示」路径（只读 mock / 不提交），并说明为何不自动执行写操作。

参考实现：`src/pages/imageCompressPage.ts`、`src/pages/imageCropPage.ts`（初始化末尾 `loadSample()`）。

## 上线前 Checklist（PR 合并前）

- [ ] **清单前检索覆盖优化已做**（slug/title/关键词/desc；`02`/`notes` 有覆盖表含逐词落点；见 `tool-i18n-localization.mdc` 步 0b）——**勾本 Checklist 之前必须已完成**
- [ ] `src/site/tool-catalog.d/{slug}.json` 完成；`related` ≥ 2；纯本地处理默认 `localProcessing: true`（边缘请求工具须 `false`，首页不显示「不上传」标签）
- [ ] `npm run coverage:gate -- --slug={slug} --phase=all` 通过（0b + 步2/1b + 步4/2b）
- [ ] 已 `npm run build:site`（含 merge；`public/_pages/*/index.html` 与 sitemap 已含本工具；勿只跑 `merge:tools`）
- [ ] title 非参数枚举目录腔；`02` 覆盖表填满且 `03` 勾选清单前覆盖与「检索覆盖已优化」
- [ ] **相关关键词已落入** H1 / description / FAQ / Use cases；无页上关键词列表；**每语已按当地搜法重跑**（非 en 直搬）
- [ ] 未手改生成物（catalog.json / `i18n/{lang}.ts` / registry / i18n-tools.generated）
- [ ] 未改其他工具分片或 `src/index.ts`（除非 `CROSS_TOOL_UPDATE=1`）
- [ ] `TOOL_SLUG={slug} npm run lint:tool-isolation` 通过
- [ ] `public/icons/tools/{slug}.svg` 可在浏览器打开（无 xmlParseComment）；注释 ASCII
- [ ] 10 语言 title / description / article / example 齐全（写在 `i18n/tools/{slug}/`）
- [ ] 十语已按 `tool-i18n-localization.mdc` **稳妥常态**做检索向独立重写（非脚本同构灌语），并完成 ≥3 轮核查；「待母语抽查」已清
- [ ] **文案生成后覆盖优化已做**（母版步 2 + 抽查语步 4）；无参数枚举 H1；次要意图有落点
- [ ] `README.md` 工具清单已更新（中英各一条，与 catalog 一致；写入前已过清单前覆盖）
- [ ] 本地打开 `/` 与 `/zh/`（或目标语首页），featured/all-tools 可见本工具入口
- [ ] **进页自动跑样例并显示真实结果**（Load sample 与自动路径一致；见上节）；例外已在 `02` 注明
- [ ] `npm run lint:seo` 通过
- [ ] IG ≥ 3 项（§3.1），PR 写明对应章节
- [ ] 长尾：写入 Use cases/FAQ，或满足拆页条件并附理由
- [ ] 文件/粘贴类：本地处理 FAQ 或声明
- [ ] JSON-LD 与可见文案一致；无虚假评分

## 详细 SEO

见 `seo-google-policy.mdc`、`tool-i18n-seo.mdc`、`tool-i18n-localization.mdc`、`docs/SEO_TOOL_RULES.md`、`docs/2026-07-28-google-seo-strategy-implementation.md`。
