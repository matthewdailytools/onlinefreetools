---
description: 工具与 topic 多语言须按检索习惯本地化；Title 禁参数枚举、禁 page 当产品名、场景须写全；禁止脚本批量同构机翻；≥3 轮核查
globs: src/site/i18n/*.ts,scripts/site/i18n.mjs,scripts/site/topic-ig-copy*.mjs,scripts/site/i18n-topics*.mjs,scripts/site/i18n-topic-ig.mjs,work-tasks/**/*
alwaysApply: false
---

# 翻译本地化与多轮核查

**目标**：每语文案像当地人会搜、会读的工具站，不是英模直译。依据：策略 §5.5；字段/lint 见 `tool-i18n-seo.mdc`。立项填空：`work-tasks/_template/03-locale-briefs.md`。

**权威**：本文件是 Agent 可执行落地；与 `seo-google-policy.mdc` / `lint:seo` 冲突时以上层为准。`lint:seo` 通过 **≠** 本地化完成。

**机器门禁（辅助，不能替代本文件的实质分析）**：

- `npm run coverage:gate -- --slug={slug} --phase=0b|2|4|all`（`scripts/validate-tool-coverage-rounds.mjs`）——验收 `02` 覆盖表与 `03` 多轮行 `0b` / `1b` / `2b`（日期 + ≥20 字摘要；步 4 须列出 en,zh,es,ja）。
- `npm run lint:seo` 另含 title 参数枚举启发式，并对**已写「清单前检索覆盖」专节且 ready/implemented** 的工具跑 `phase=all`。旧工具无专节的目录腔 title 仅 WARN。
- Agent 执行顺序见 [`.cursor/skills/tool-coverage-pass/SKILL.md`](../skills/tool-coverage-pass/SKILL.md)。

通过门禁仍须完成步 0b / 2 / 4 的**实质**检索判断与文案回写（禁止只勾选）。

---

## 稳妥常态工作流（默认必须）

按顺序执行；**禁止跳步用脚本一次灌满十语上线**。

| 步 | 做什么 | 产出 / 门禁 |
|---|---|---|
| 0 | 填 `work-tasks/{slug}/03-locale-briefs.md`（每语检索词、语气、title 方向、按钮说法、SERP 摘录）草稿 | brief 空白则停 |
| **0b** | **清单前检索覆盖优化（硬门禁）** — 见下节「清单前…」 | 覆盖表写入 `02`；`03` 行 `0b`；`coverage:gate --phase=0b` 绿 |
| **0i** | **用户意图审查并优化（硬门禁）** — 见 `work-tasks-tool-brief.mdc` | 审查表写入 `02`/`notes`（满足/超出/缺口+已回写）；未写不得 `02`→`ready` / 勾工程清单 |
| 1 | 写 **一种** 母版语（通常 en 或 zh）完整键：title / description / How / Rules / Example / Use cases / FAQ / UI | `lint:seo` 对该母版过；记轮次 1 |
| 2 | **Title / 描述 / 检索词覆盖优化（母版 · 生成后）** — 须在步 1 生成之后再跑一遍 | 改母版 title/desc；`03` 行 `1b`；`coverage:gate --phase=2` 绿 |
| 3 | **逐语**（或每批 ≤3 语）按该语 brief **独立重写**；同步首页主词（生成分片） | 禁止 `Translate to {lang}`；每语 title/FAQ 句式不得与英模锁死同构；记轮次 2 |
| 4 | **Title / 描述 / 检索词覆盖优化（抽查语 · 生成后）** — 对 en + zh + ≥2 他语再跑一遍 | `03` 行 `2b`（须含 `en,zh,es,ja`）；勾选「检索覆盖已优化」；`coverage:gate --phase=4` 绿 |
| 5 | 禁词表全扫 + 抽查 **en + zh + 至少 2 个其他语**（优先含 ar/ja/ru 若已上线） | 再 `lint:seo`；记轮次 3；`03` 勾选轮次框 |
| 6 | 定稿 | `03` 状态 → `i18n-done`；`coverage:gate --phase=all`；若仍有母语不确定项 → 保留「待母语抽查」且不得假装已清 |

**允许**：脚本/补丁 **只做已定稿文案的落盘**（把审过的字符串写入 `.ts`）。  
**禁止**：把「生成八语同构段落」的流水线脚本当作本地化步骤（见下节红线）。  
**禁止**：跳过步 **0b** / **0i** / 步 2 / 步 4，把「首稿 title + lint 绿」或「未分析就勾清单」当作检索覆盖或意图审查已完成。

Agent 提示应类似：`Write {lang} copy for users who search: …`（附该语 brief），**不要** `Translate the following to {lang}`。

---

## 相关关键词落地（硬性 · 两条）

覆盖分析（清单前 0b + 步 2/4）产出的**主词 / 次词**必须同时满足：

### 1. 必须落入可见文案（禁止只留在 brief）

| 词类 | 必须落到的可见键 / 区块 | 禁止 |
|---|---|---|
| **主词**（每语 ≥1） | `tool_*_title`（H1） | 只写在 `work-tasks`、只进 meta 不进 H1 |
| **次词**（brief 中未进 H1 的高流量说法） | `tool_*_description` 前半，和/或 FAQ（`*_faq_q/a*`），和/或 Use cases（`*_usecase_*`）；必要时 How 首段 | 单独「相关关键词 / Related keywords」列表或标签云；为塞词拆近义 URL；keyword stuffing 式罗列 |

**自检**：打开该语工具页，能指出「每个覆盖词出现在哪一句」；说不出落点 = 未完成。  
**对齐 Google**：[Keyword stuffing](https://developers.google.com/search/docs/essentials/spam-policies#keyword-stuffing)——成块关键词列表视为 spam 风险；须自然写进对人有用的句子。

### 2. 必须按各语言检索习惯重跑（禁止英模直搬）

- **每语独立**：在写/改该语 i18n 前，于 `03-locale-briefs.md` 写该语 **3–5 个当地检索词**（用户会搜的说法，不是 en 词的字面翻译）。
- **重跑时机**：步 0b（母版方向）→ 步 3（逐语重写）→ 步 4（抽查语再优化）。**每语**都要对照当地搜法更新主词/次词与落点；禁止「en 覆盖表译完即完」。
- **产出**：覆盖表或 `03` 多轮行须能看出「语种 → 主词/次词 → 落在 title|desc|faq|usecase 哪一键」；步 4 的 `2b` 摘要须点明抽查语（默认含 `en,zh,es,ja`）的检索向改动。
- **禁止**：十语共用同一组英文关键词骨架；他语 title/FAQ 与 en 锁死同构只换词。

---

## 清单前检索覆盖优化（硬门禁 · 生成清单前必做）

**「清单」指**：`02`「页面模块清单」、`tool-creation.mdc`「工程清单」落地与「上线前 Checklist」勾选、以及 README「工具清单 / Tools List」写入。  
**时机**：**在勾选或生成上述任一清单之前**，必须再对 **slug / title / 关键词 / desc** 做一轮「是否符合用户搜索习惯 + 关键词覆盖」的分析，并据此优化；**未完成不得进入清单勾选，`02` 不得标 `ready`。**

本步是 Agent（或人工）按下列清单执行的分析与回写，**不是** `lint:seo` 自动替代；仓库无独立 SERP 爬虫时，仍须对照 `03` brief 检索词、方向文档意图词与已知 SERP 习惯完成分析。

**对象（须四项都过）**：

| 字段 | 分析要点 | 优化动作 |
|------|----------|----------|
| **slug** | 是否为用户**实际使用场景**会搜/会点的 kebab 说法（情境+动作，平台只是情境一种）；是否 doorway/近义薄页；与 related、清单已占用 slug 是否撞车；禁止品类头词与 `*-pack` | 必要时改 slug（同步路径/brief 夹名）或合并意图到一带多场景；默认预设须等于 H1 场景 |
| **title**（H1 方向） | 主检索词是否落在 H1；是否参数枚举目录腔；是否当地口语 | 改成结果/场景向；主词进 H1 |
| **关键词**（brief 3–5） | 是否覆盖主意图 + 次要高流量说法；是否堆砌无关词；**若有 Google/Bing Keyword Planner 长尾归属本 slug，须并入覆盖词** | 收敛/补齐；**标明每个词落入 H1 / desc / FAQ / Use cases 哪一处**（见上节「相关关键词落地」） |
| **desc**（description 要点 / 首页 `_desc`） | 是否含步骤或结果 + 示例信号；次词是否有落点；是否空话口号 | 加长检索向描述；次词写入前半或 FAQ |

**清单（逐项过并记录）**：

1. 用户会搜什么？主词是否进 **title/H1**（指出具体措辞）。
2. slug 是否与主意图一致、是否需改（改则先改再写清单）。
3. 次要关键词是否都有落点（**description / FAQ / Use cases**）；禁止为塞词拆近义 URL；禁止只列词不写句。
4. title 无参数枚举；desc 非空壳「free online best」。
5. 与 related / 邻近工具 intent 边界清楚。
6. **（若有）Ads/Planner 长尾**：归属本 slug 的长尾已融入文案，且 `02` 交互规格 / 功能能完成对应任务（见 `work-tasks-tool-brief.mdc`「Ads / Keyword Planner 长尾融入」）；仅出现词无能力 → 不合格。

**产出（缺一不可）**：

- 在 `02-tool-info.md`（推荐专节）或 `notes.md` 写 **覆盖表**：`主词→H1` / `次词→desc·FAQ·Use cases`（逐词落点） / `slug 结论（保留|已改）` / `优化摘要`（含日期）。
- 有 Planner 分析时另填 `02`「Ads / Keyword Planner 长尾」表（文案落点 + 功能覆盖）；无则勾「不适用」。
- 回写 `02` SEO 卡片的 Title、Description 要点与建议 slug；同步 `03` 母版语检索词与 Title 方向（若有改动）。
- `03` 勾选「清单前检索覆盖已做」。

---

## 用户意图审查并优化（硬门禁 · 标 ready 前必做）

**时机**：清单前覆盖表起草之后、勾页面模块清单 / 标 `02`=`ready` **之前**（立项步 **0i**）。完整对照表与结论类型见 `.cursor/rules/work-tasks-tool-brief.mdc`「用户意图审查」。

覆盖表只保证「词有落点」；本步保证「搜主词的人能办成事」，并拦住做多。

**必问**：

1. `00` 要建的东西，现在的 How / 首屏 / 样例是否仍是那件事？
2. 主检索词用户的任务是什么？How 是否先答该任务，而不是先答「怎么点本站按钮」？
3. 有无超出：次计算器抢首屏、Formula/FAQ 做成第二工具、desc 写 SEO/工程自述或作业器免责堆砌？
4. 词表里不同意图的头词是有意不满足还是误改了 H1？
5. **（若有）Ads/Planner 归属长尾**：搜这些长尾的人能否在本页办成对应任务（功能覆盖，不只是文案出现词）？

**产出（缺一不可）**：`02`「用户意图审查」表（或 `notes.md` 同题专节）含**总判**（满足 / 部分满足 / 超出 / 有意不满足）+ 满足/超出/缺口与已做优化；部分满足或越界必须已回写 How/交互/FAQ/desc。有 Planner 时须含长尾任务核对。`03` 勾选「用户意图审查已做」。

---

## Title / 描述 / 检索词覆盖优化（文案生成后必做 · 步 2 / 步 4）

**时机**：在 **i18n 文案**（title、description、首页 `_title`/`_desc`、brief 检索词）**已经写出之后**再执行；**不能代替**上节「清单前」门禁。步 2 = 母版生成后；步 4 = 抽查语重写后。

**对象**：`tool_*_title`、`tool_*_description`、首页 `_title`/`_desc`（生成分片或 `i18n-tools.generated` 来源）、FAQ/Use cases（次词落点）；同步回写 `03-locale-briefs.md`。

**清单（逐项过）**：

1. **主意图是否在 H1**：brief 里 3–5 个检索词中，至少 1 个主词能指出落在 title/H1 的哪里。
2. **去参数枚举**：副标题若是控件目录 → 改成结果/场景向（见「Title 自然口语」）。
3. **次高流量词落点**：未进 H1 的次要说法 → description 前半 **和/或** FAQ **和/或** Use cases；**禁止**为塞词拆近义 URL、把模式名堆进 H1、或单独关键词列表。
4. **intent 边界**：与 catalog `related`、邻近工具 H1 不撞车。
5. **首页短 title**：短检索主词；补词优先改 `_desc`。
6. **按语重跑（强制）**：步 4 对每抽查语用**该语**检索词再核落点；不得沿用 en 词表翻译验收。抽查语 title 不得共用同一英模骨架。

**产出**：在 `03`「多轮记录」增加一行（日期 + 优化摘要，须含「哪些次词写入了 desc/FAQ/usecase」）；可更新覆盖表。未做步 2/4 不得标 `i18n-done`。

---

## 写法

- **重写非翻译**：先定该语 3–5 个搜索词，再写 title / H1 / description / 按钮；**相关关键词须按该语习惯重跑**（见「相关关键词落地」§2）。
- **Title / H1 自然口语（强制）**：主标题写「用户要办的事 + 结果/场景」，**不要**把控件参数名用顿号/逗号串成副标题。控件名（边长、画质、目标 KB、质量滑条等）放 **UI 标签** 或 How/Rules，不堆进 H1。
  - **反例（机械）**：`图片压缩 — 最长边、质量与目标体积（本地处理）`；`Image Compressor — Max Edge, Quality & Target Size Local`
  - **正例（自然）**：`图片压缩 — 缩小尺寸与文件体积，不上传服务器`；`Image Compressor — Shrink photos on your device`
  - 自检：删掉副标题后，主词是否仍是当地人会搜的说法？副标题读起来像不像产品说明书目录？
- **产品类型用检索词，不用站点 chrome（强制 · 工具页与 `/topics/{id}` 叶子 H1 均适用）**：
  - Title/H1 写用户会搜的**产品类型**（prompt **builder / generator**、计算器、对比工具、压缩器），**禁止**用 `page` / `页` / `página` / `Seite` / `страница` / `halaman` / `ページ` 当产品名（「which prompt page」「选哪张提示词页」= 机翻 + 站内导航问句，不是检索向）。
  - 决策表/流程里可以用「这页」指具体 URL；**H1 与 `<title>` 不行**。
  - 场景簇（多 slug 对应不同控件/产物）：H1 须把**独立场景说全**，或用能覆盖全集的分组（写作 / Midjourney / 线稿 / 电影 / 短剧 / 产品设计 / Android / iOS）。**禁止**为套「A、B, or C」模板只抽 3 个场景。
  - **禁止**先写中文「选哪张 X 页」再逐语填空；他语须按当地人搜 builder/generator 的说法独立重写。
  - 反例：`Writing, Midjourney, or iOS agent: which prompt page`；正例：`AI prompt builders and generators for writing, Midjourney, sketch, film, short drama, product design, Android, and iOS`。
  - Topic 叶子 H1 **不是**强制「决策问句」；问句仅当问的是用户任务（先算 BMI 还是 TDEE），不是「选哪张站内页」。
- **「本地处理 / 不上传」隐私表述（强制）**：凡声明浏览器内处理的工具（文件、粘贴、PDF、图片等），**desc / FAQ / `home_cat_*_blurb` / 首页 `_desc`** 须用该语用户能立刻听懂的说法，说清「文件留在设备/浏览器」且「不上服务器」。详见下节。
- **UI 短词本地说**；示例可本地化；技术专名（CRLF、`.env`、Markdown、HTML）可保留。
- **首页** `scripts/site/i18n.mjs` 主词与工具页一致。
- **反同构**：FAQ 题序、How/Rules 开篇、Use cases 切入点应随语种变化；禁止十语同一「Process: … Example: …」骨架只换词。
- **只写用户视角**：面向用户的正文（description/how/rules/example/usecases/faq）只写「用户完成任务需要知道什么」。**禁止**把 SEO/工程理由（doorway、空壳页/薄页、thin URL、slug、「双向放同一地址」「不拆 URL」「不是又一个…页」等自我指涉/元叙述）或实现库名（js-yaml、papaparse…）当卖点写进用户文案；此类理由只属于 `work-tasks/` 与 `docs/`（或代码注释）。母版语（en/zh）也须遵守——母版若混入元叙述，会被逐语放大成译腔。
- **忌说明书腔**：How/Rules 少写实现术语（`toBlob`、质量二分、Canvas 流水线）当面向用户的主句；改成「缩小、重画、再导出」「逐步降低画质去逼近」等可观察说法。库名/API 名最多进 References。
- **禁止关键词展示模块**：不得为 SEO 增加「相关关键词」可见列表；覆盖词只进自然语言句子。
## 隐私本地处理表述（强制）

中文用户常搜/常读 **「不上传服务器」**；其他语言**不能**只写含糊的 `local` / `lokal` / `en local` / `محلياً` / 半截 `no upload` / `sans envoi` / `sem upload` / `ohne Upload`——当地人未必理解为「文件不上你们的服务器」。

**须同时表达两层（至少一层用该语完整说清，另一层可紧挨同句）**：

1. **位置**：文件留在设备 / 浏览器标签页（on your device / en el dispositivo / auf dem Gerät / 端末内 …）  
2. **边界**：不上传到服务器（not uploaded to a server / sin subir al servidor / ohne Server-Upload / サーバーにアップロードしない …）

**十语默认说法（可按语气微调，禁止整语只留「local」一词）**：

| 语 | 推荐表述 |
|---|---|
| zh | 不上传服务器；文件不出本机 / 浏览器内处理 |
| en | files stay on your device; not uploaded to a server / never leave your device |
| es | sin subir al servidor; no salen del dispositivo |
| ar | دون رفع إلى خادم؛ تبقى على جهازك |
| pt | sem enviar ao servidor; ficam no dispositivo |
| id | tanpa unggah ke server; tetap di perangkat |
| fr | sans envoi au serveur; restent sur l’appareil |
| ja | サーバーにアップロードしない；端末内で処理 |
| ru | без загрузки на сервер; остаются на устройстве |
| de | ohne Server-Upload; Dateien bleiben auf Ihrem Gerät |

**适用键**：工具 `*_desc` / `*_description` / FAQ「是否上传」；分类导语 `home_cat_*_blurb` / `home_cat_*_desc`；页面 How/Rules 隐私句。  
**诚实边界**：库脚本可从 CDN 加载时，FAQ 可一句说明「库可能从 CDN 加载，**你的文件**仍不上传」——勿用「100% offline」等虚假承诺。  
**自检**：把「local/本地」划掉后，读者是否仍能判断「会上服务器吗？」不能 → 须改写。

### 首页工具卡片「不上传」标签（强制）

纯本地处理用户内容的工具，在首页最新上线 / 分类工具卡片标题下显示短标签（中文 **「不上传」**；他语见 `home_tag_no_upload`），并**链接**到该语隐私政策本地处理节：`/{lang}/privacy#local`（默认语路径无前缀时仍用显式语言前缀）。

| 项 | 约定 |
|---|---|
| Catalog | `localProcessing: true`（缺省亦视为 true）；边缘请求类工具须 **`localProcessing: false`**（当前：`website-headers`、`ip-address`、`indexnow`） |
| 渲染 | [`scripts/site/components/content-home.mjs`](../../scripts/site/components/content-home.mjs) → `.tool-no-upload-tag` |
| 文案键 | `home_tag_no_upload`（短标签）+ `home_tag_no_upload_title`（`title` 提示：设备 + 不上服务器 + 说明入口） |
| 样式 | `public/styles/site.css` `.tool-no-upload-tag` |
| 交付 | 新工具默认本地 → 卡片自动出标签；若会把用户内容发到 Worker/第三方，须在分片写 `localProcessing: false` 且页内诚实说明 |

**禁止**：边缘工具误挂「不上传」标签；标签文案只写含糊 `local` 而无 `title`/链接触达「不上服务器」说明。

## 每语 brief（写 i18n 前必填）

每个目标语一份（可复制 `03-locale-briefs.md`）。禁止只喂英文全文要求「translate to X」。

```markdown
## Locale brief — {lang}
- 该语检索词（3–5）：
- 语气：口语工具站 / 偏正式开发者（择一）
- Title / H1 方向（本地说法，非英译；**结果/场景向**，勿「参数A、参数B、参数C」目录腔）：
- 按钮短词（Compare/Clear/按行… 的本地说法）：
- 本地示例用词（可选）：
- 可保留英文/符号：
- 本工具额外禁用（可选）：
- SERP 竞品 title 摘录（1–3）：
- 轮次2已重写：[ ]  轮次3已抽查：[ ]
- 待母语抽查：[ ]   # 仅 ar/ja/ru 或抽查存疑时勾选；清掉前须有实质重写或真人确认
```

---

## 收紧合规（相对「先 en/zh 再脚本灌其余语」）

以下视为 **不合规**（即使 `lint:seo` 绿）：

1. **批量同构**：一次生成 ≥4 语，且 title/FAQ/How 与英模句式锁死平行（同问题顺序 + 同连接词骨架）。
2. **跳过 brief**：`03` 空白或仅复制英检索词未写本地 title 方向就开始写 i18n。
3. **假本地化**：非 en/zh 键缺失，靠 `t()` 回退英文上线。
4. **用 lint 代替抽查**：未完成轮次 3 抽查就标 `i18n-done`。
5. **空勾「待母语抽查」**：标了待抽查却无后续重写/确认记录，或未清标记却宣称定稿。
6. **参数枚举式 title**：H1/title 用顿号/逗号罗列控件名或工程参数（「最长边、质量与目标体积」「Max Edge, Quality & Target Size」），读起来像设置面板目录而非检索向口语。
7. **跳过检索覆盖优化**：未做「清单前」slug/title/关键词/desc 分析（步 0b），或 title/description 已生成却未执行步 2 / 步 4，就勾工程/上线清单或标 `i18n-done`。
8. **跳过用户意图审查**：未对照主词搜索者任务判断满足/超出并回写 How/交互，就标 `02`=`ready`（覆盖表绿 ≠ 意图已对齐）。
9. **含糊本地/隐私表述**：文件类工具用 `local` / `本地处理` / 半截 `no upload` 等，未按「隐私本地处理表述」说清设备与服务器边界。
10. **H1 用 page 当产品名 / 场景抽签**：工具或 topic 叶子 title 写成 `which X page` / 「选哪张 X 页」，或场景簇只点 3 个成员套「A、B or C」模板（见写法 · 产品类型）。

**收紧后的通过标准**：

- 每语能对照 brief 指出「检索主词落在 title/H1 的哪里」。
- 任意抽两语 FAQ，读起来不像同一段英模换皮。
- `03` 中「待母语抽查」：**有则必须处理**；处理方式为（a）按 brief 再重写该语关键段落后 **清掉勾选**，或（b）真人母语确认后清掉并在多轮记录注明。不得「带勾上线」。
- `03` 已勾「清单前检索覆盖已做」与「检索覆盖已优化」（步 2+4），且 `02`/`notes` 有覆盖表；新立项另须有「用户意图审查」总判；多轮记录含摘要。

---

## 禁词 / 禁模式表

| 类型 | 禁止（示例） | 改为 |
|---|---|---|
| 英模造词 | granularity、对比粒度、Diff-Granularität、Granularitas | 按行/按词；Compare by；比較単位 |
| 错义借译 | es `altas y bajas`=增删；乱造 `*hunk*` 德语词 | 当地「新增/删除」 |
| 未写完缩写 | `Cars.` | Character / Carácter / 字符 |
| 目录腔标题 | ユースケース、Casos de uso 堆砌英文 Use cases | 「适合什么场景」「こんなときに」 |
| **参数枚举 title** | `… — 最长边、质量与目标体积（本地处理）`；`… — Max Edge, Quality & Target Size` | 结果/场景向：`… — 缩小尺寸与文件体积，不上传服务器`；参数名留给 UI |
| **含糊「本地」隐私句** | 仅写 `local` / `lokal` / `en local` / `本地处理` / `ohne Upload` / `sans envoi` / `sem upload` / `tanpa unggah` 且不提设备或服务器 | 该语完整说清「留在设备」+「不上服务器」（见上节十语表） |
| 说明书腔正文 | How 主句堆 `toBlob` / 质量二分 / Canvas 流水线 | 「缩小、重画、再导出」；API 名进 References |
| 同构机翻 | 十语同一句式 + 同一 Hello world 骨架且无本地主词 | 每语独立重写 title/FAQ |
| 假本地化 | 仅 en/zh 有文案，其余靠 `t()` 回退英文 | 十语键齐全 |
| 脚本灌语 | 无逐语 brief 审阅的「一键八语」补丁当交付 | 逐语/≤3 语一批；脚本仅落盘 |
| 内部理由外泄 | 用户文案出现 doorway / 空壳页 / 薄页 / URL puerta / slug / 「只改标题」「不拆 URL」「为什么放一页」 | 删除；理由移到 `work-tasks` / `docs` |
| 元叙述 / 直译腔 | 「跟…走」「类型行为」「这不是又一个薄页」「We keep both directions … so … doorway」 | 用该语自然说法，正文只描述工具行为 |
| 库名当卖点 | 面向用户写「经典 js-yaml 行为」「用 papaparse」作主张 | 描述可观察行为（如「未加引号的值会被判定类型」）；库名留 References |
| **关键词列表 SEO** | 页上「相关关键词」块、标签云、无语境词串 | 主词→H1；次词→desc/FAQ/Use cases 自然句 |
| **英模关键词直搬** | 他语沿用 en 词表不做当地检索重跑 | 每语 brief 3–5 当地词 + 步 3/4 重跑落点 |
| **page 当产品名** | `which prompt page`；「选哪张提示词页」；`quelle page de prompt` | 当地检索类型：prompt builder / generator、构建器与生成器 |
| **场景抽签 H1** | 九个场景只写 Writing、Midjourney、iOS 套「A, B, or C」 | 独立场景说全，或用能覆盖全集的分组 |

技术词（diff、JSON、CRLF）在开发者工具中**可以**保留，但不可代替本地主 title。

## 多轮核查（≥3）

| 轮 | 做什么 | 通过 |
|---|---|---|
| 1 | **清单前覆盖（步 0b）** + **意图审查（步 0i）** + 母版初稿；`lint:seo`；**紧接母版检索覆盖优化（步 2）** | 覆盖表齐全；意图总判已写且 How 对齐主任务；≥120、FAQ≥3；主词在 H1；次词在 desc/FAQ/usecase；无参数枚举 |
| 2 | 按每语 brief **独立重写**（非批量同构）；**每语按当地搜法重跑关键词**；同步首页；**抽查语再跑检索覆盖优化（步 4）**；对照禁词表 | title=检索向；次词有落点；无英模词表直搬 |
| 3 | 抽 en+zh+≥2 他语：title/按钮/FAQ/隐私；句式是否同构；title 是否目录腔；再 `lint:seo` | 无译腔/错义/同构/参数枚举 title；待母语抽查已清或已排期 |

## 定稿自检

- [ ] 每语有 brief（**该语**检索词 3–5 + title 方向 + 按钮说法）
- [ ] title/H1 为结果或场景向口语，**非**「参数A、参数B、参数C」目录腔（见写法 · Title 自然口语）
- [ ] title/H1 产品类型是用户搜法（builder/generator 等），**不是** page/页；场景簇已写全，非「A、B or C」抽三个
- [ ] **相关关键词已落入** H1 / description / FAQ / Use cases（能指出每词所在句）；无关键词列表模块
- [ ] **每语已按当地检索习惯重跑**关键词（非 en 直译/直搬）
- [ ] **已做清单前检索覆盖优化**（步 0b：slug/title/关键词/desc；`02`/`notes` 有覆盖表含逐词落点）
- [ ] **已做用户意图审查**（步 0i：满足/超出总判；How 先答搜索者任务；越界已划边界并回写）
- [ ] **已做文案生成后覆盖优化**（母版步 2 + 抽查语步 4）；`03` 多轮记录有摘要
- [ ] 对照禁词表扫过 title/按钮/FAQ
- [ ] 文件/粘贴类：隐私句已按「隐私本地处理表述」写清设备 + 不上服务器（非含糊 local）
- [ ] ≥3 轮且 2/3 轮有实质修改（非只改标点）
- [ ] 无未清的「待母语抽查」勾选（或已在 `03` 多轮记录写明排期与责任人）
- [ ] `lint:seo` 通过（≠本地化完成）
- [ ] 未使用「英模 → 脚本灌其余语」作为唯一交付路径
- [ ] 可见正文无 SEO/工程元叙述（doorway/空壳/薄页/slug/自述「单页/不拆 URL」）与库名卖点；理由仅在 `work-tasks`/`docs`
- [ ] How/Rules 无把实现 API/`toBlob`/算法名当用户主句的说明书腔

## 红线

- 禁止批量直出十语不经 brief/重写/禁词核查即上线
- 禁止用英文回充冒充多语言完整
- 禁止将「生成同构多语文案」的自动化脚本当作合规本地化
- 禁止带着未处理的「待母语抽查」标记宣称 `i18n-done`
- 禁止跳过「清单前检索覆盖优化」（步 0b）就勾页面/工程/上线清单或标 `02`=`ready`
- 禁止意图 slug 已有 Google/Bing Ads（Keyword Planner）长尾分析，却未融入文案或未让功能覆盖长尾任务，就标 `02`=`ready`
- 禁止跳过「用户意图审查并优化」（步 0i）就标 `02`=`ready`（覆盖表绿 ≠ 主词任务已对齐）
- 禁止跳过「Title / 描述 / 检索词覆盖优化」（母版步 2、抽查语步 4）就标 `i18n-done`
- 禁止文件类工具只用含糊 `local` / `本地处理` / 半截 `no upload` 等冒充隐私说明（须对齐「隐私本地处理表述」）
- 禁止覆盖关键词只写在 `work-tasks` 而未回写到 H1/description/FAQ/Use cases
- 禁止页上增加「相关关键词」列表/标签云作 SEO
- 禁止他语沿用 en 关键词表而不按当地用户搜索习惯重跑
