---
description: 工具 i18n 必填字段、Information Gain、FAQ 与 meta description 校验
globs: src/site/i18n/*.ts,scripts/validate-tool-seo.mjs,scripts/site/topic-ig-copy*.mjs,scripts/site/i18n-topic-ig.mjs,docs/SEO*.md
alwaysApply: false
---

# 工具 i18n 与 SEO

**权威序**（冲突时）：Google 官方（`seo-google-policy.mdc`）→ 运行代码 / `lint:seo` → 本目录 rules → `docs/*`。

展开说明：`docs/2026-07-28-google-seo-strategy-implementation.md`；lint 摘要：`docs/SEO_TOOL_RULES.md`（二者须对齐 rules，不得反向覆盖）。

**Information Gain（IG）定位**：相对 SERP 同类页的新增、可验证信息；是 **内容策略原则**（对齐 Helpful Content），**不是**已确认的独立排名因子。

## 每语言必填字段

| 键 | 用途 |
|---|---|
| `tool_*_title` | 工具名称 |
| `tool_*_description` | meta / OG 描述 |
| `tool_*_article` | 页内详细说明（How / Use cases / 边界等） |
| `tool_*_example` | 简短输入→输出示例 |

`tool_*_description` 必须包含（按语言本地化）：

- 计算/处理**步骤或公式**
- 至少一个**示例或应用场景**
- 长度 **≥ 120 字符**（Bing 等会标记过短 meta；HTML head 建议 120–160）

## 相关关键词（强制 · 详见 localization）

完整门禁见 `tool-i18n-localization.mdc`「相关关键词落地」：

1. **落入文案**：主词 → H1（`tool_*_title`）；次词 → `description` / FAQ / Use cases 自然句。禁止页上「相关关键词」列表。  
2. **按语重跑**：每语按当地用户搜索习惯重定 3–5 词并回写落点；禁止英模关键词表直搬/直译验收。

## Title / H1：产品类型与场景（工具页 + topic 叶子）

与 `tool-i18n-localization.mdc`「产品类型用检索词」同条，摘要：

- 用户搜的是 **工具类型**（builder、generator、calculator、compare），不是「哪张 page」。
- Topic 叶子若对应多场景 URL：H1 须覆盖全部独立场景（或完整分组），禁止为「决策问句」模板只点 3 个。
- 机器门禁：`lint:seo` → `validate-topic-leaf-titles.mjs`（chrome 名词 + prompt 簇场景覆盖）。

## Information Gain 最低配额（上线硬性）

上线前至少满足 **§3.1 九维中的 3 项**，并在 PR 写明「页面哪一节体现」：

| # | 维度 | 要求摘要 |
|---|---|---|
| 1 | 公式/规则 | 计算式、字段映射、算法步骤 |
| 2 | 边界/失败 | 非法输入、局限、常见坑 FAQ |
| 3 | 场景语境 | Use cases 绑定岗位/流水线 |
| 4 | 对照表 | 单位假设、限制对照 |
| 5 | 权威引用 | 可点击 References（YMYL 建议 ≥2） |
| 6 | 本地隐私 | 文件/粘贴类：浏览器处理、不上传声明 |
| 7 | 多语言 | 各语检索向重写 + brief/禁词/≥3 轮（见 `tool-i18n-localization.mdc`）；非英模直译 |
| 8 | 数值示例 | ≥1 组 Input→Output（含边界例更佳） |
| 9 | 主题内链 | `tool-catalog.json` → `related` ≥ 2 |

另须：

- 可见 **How it works**：`how_body`（总答）+ **`how_item_1…n` 有序操作步骤**（建议 3–6；动词开头、与 UI 一致；渲染为 `<ol>`）。凡可交互工具均须有步骤（含计算器：填字段 → 选模式 → 计算 → 读结果）。存量仅有散文 `how_body` 的页在改文案时迁移。
- 至少 **1** 个完整 Example（计算/转换类加 Formula/Rules；Rules 写约束，勿假扮操作步骤）
- **FAQ ≥ 3**（YMYL 须含局限性/免责类问题）
- 动态工具结果的关键解读须落到可索引 **Example / FAQ**（勿仅依赖交互态文案）
- **不为**进 Bing/Google AI 摘要单独加 HowTo schema / `llms.txt` / AI 专用切块；步骤为人服务，抽取是副产品

**禁止零增益**：仅写 `Upload → Click → Download` 口号而无真实控件对应，或「快、安全、免费」空话。

## 长尾与拆页

- **默认**：同一工具页用 Use cases + FAQ 覆盖多场景（一带多场景）
- **可拆独立 URL**：功能、参数、算法或正文有实质差异，且独立满足 IG 配额
- **禁止**：仅 title/H1 换词的空壳长尾变体（doorway / scaled content）

拆页前自检：去掉 title 后正文是否仍明显不同？是否有独立 Example/边界？会否形成 doorway？

## FAQ 与 YMYL

- 每工具 ≥ 3 组 `tool_*_faq_qN` / `tool_*_faq_aN`，**渲染在正文**（不对爬虫隐藏）
- **不以** FAQPage / HowTo / Featured Snippet / PAA 展示为 KPI（问答仍为人服务）。注意 FAQ 富结果已于 **2026-05-07 全站退役**、HowTo 2023-09 桌面弃用；`FAQPage`/`HowTo` schema 仍有效可保留（Google 用于理解页面）但不再产出富结果（见 `seo-google-policy.mdc` / `seo-html-defaults.mdc`）
- `tool-catalog.json` 中 `ymyl: true`：必须 `tool_*_disclaimer`；建议 `tool_*_references`

## 验证

```bash
npm run coverage:gate -- --slug=<slug> --phase=0b|2|4|all   # 分阶段检索覆盖产物
npm run lint:seo    # 含 title 启发式 + 对有覆盖专节工具跑 coverage rounds(all)
npm run build:site  # 发布前必须构建 sitemap
```

校验失败时补文案/翻译后再合并，勿绕过检查。分阶段执行见 `.cursor/skills/tool-coverage-pass/SKILL.md`。

**本地化与多轮核查**（检索向、禁机翻、稳妥常态、**清单前 slug/title/关键词/desc 覆盖优化**、文案生成后再优化、收紧合规、≥3 轮审核）见 `tool-i18n-localization.mdc`。`lint:seo` 通过 ≠ 翻译已本地化，也 ≠ 清单前覆盖已做。禁止「母版 + 脚本灌其余语」的同构交付；禁止未做清单前分析就勾工程/上线清单；禁止首稿过 lint 即跳过步 2/4。

## 红线

- 禁止空壳页、无增量机翻铺量、批量 query 变体 doorway 页
- 禁止仅对爬虫可见的 FAQ（cloaking）
- 禁止虚假 `aggregateRating`；JSON-LD 须与可见文案一致
- 新工具必须写入 `tool-catalog.json` 且具备真实交互与增量说明
- 翻译须按当地检索与用语习惯本地化，并经多轮核查；禁止批量直出后不重写就上线（详见 `tool-i18n-localization.mdc`）
- **可见文案边界**：面向用户的正文（description/how/rules/example/usecases/faq）只写用户任务相关的规则与边界；**禁止**写 SEO/工程理由（doorway/空壳/薄页/thin URL/slug/「不拆 URL」「为什么单页」自述）或把实现库名当卖点。这类理由留 `work-tasks`/`docs`。详见 `tool-i18n-localization.mdc`。
