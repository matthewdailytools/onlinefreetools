日期：2026年1月10日23时17分
摘要：首页改为统一走动态构建流水线，并拆分 header/sidebar/footer/content 模块，补齐多语言文案。
【question】
首页也动态构建，拆分header，sidebar，footer，content模块，以及多国语言支持
【try to solve】
- 首页继续由 `scripts/build-site.mjs` 统一生成到 `public/` 与 `public/en/`（随 `siteConfig.enabledLangs` 输出）。
- 将站点组件拆分为独立模块：
  - `scripts/site/components/header.mjs`
  - `scripts/site/components/sidebar.mjs`
  - `scripts/site/components/footer.mjs`
  - `scripts/site/components/content-home.mjs`
  并由 `scripts/site/components.mjs` 统一导出。
- 首页文案全部接入 `scripts/site/i18n.mjs`，移除页面内 `lang === 'en' ? ... : ...` 的硬编码分支，确保 `zh/en` 都能生成一致结构的首页。
