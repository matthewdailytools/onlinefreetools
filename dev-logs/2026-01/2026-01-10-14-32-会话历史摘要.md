Date: 2026-01-10 14:32
Summary: Summarized early project history: logging, static site, Cloudflare workflow, and SEO next steps.

[question]
Summarize the conversation history…

[try to solve]
**Overview**
- Main goals: establish a development-log system, a static site plus Cloudflare workflow, and keep improving default agent rules and SEO.
- Phases: log rules → static home and CDN fixes → log browsing/build pipeline → SEO and visibility → multilingual planning.

**Stack**
- Backend: Cloudflare Workers (Wrangler), static assets with 404 fallback to `public/index.html`.
- Frontend: Bootstrap 5 (cdnjs), Markdown viewer (`marked` + `DOMPurify`).
- Log pipeline: source Markdown in `dev-logs/`, generated via `scripts/build-logs.mjs` into `public/devlogs/` (index + SEO).
- Rules: `.github/copilot-instructions.md` for defaults, log naming/content, SEO, and Cloudflare workflows.

**Key changes**
- Log convention: one file per Q&A; date + summary filename; `question` / `try to solve`; omit AI trailing prompts and self-create actions.
- Home/nav: static home and tool entry; CDN SRI fix; unified log entry at `/devlogs/`.
- Log browsing: `viewer.html` then script-generated HTML for pages and index.
- SEO: canonical, OG/Twitter, JSON-LD on home and log pages; `robots.txt` and `sitemap.xml`.
- Visibility: stop ignoring `.github` in `.gitignore` so default rules stay visible.

**Status**
- Static home works; nav points to `/devlogs/`; Markdown→HTML tool available.
- Sources in `dev-logs/`; output in `public/devlogs/`; build script can regenerate the index.
- Some rollbacks left mixed `public/dev-logs/` vs `public/devlogs/` paths to reconcile.

**Open work**
- Lightweight i18n (JSON dictionary + switcher) with `<html lang>` and `hreflang`.
- Expand `sitemap.xml` (historical note; later policy excluded `/devlogs/` from indexing).
- Automate log write + `build:logs`.

**Suggested next steps**
- Implement i18n on home and log index; inject titles/descriptions and `hreflang`.
- Standardize on `public/devlogs/`, remove legacy `public/dev-logs/`, rebuild index/sitemap.
- Run log build and local preview to verify pages and SEO tags.
