Date: 2026-08-07 14:31
Summary: Completed the image-crop tool brief (work-tasks) including ten-locale briefs.

[question]
立项：图片尺寸裁剪

[try to solve]
Briefing complete under `work-tasks/image-crop/`:

- **slug / path**: `image-crop` → `/tools/image-crop` (category `image`)
- **direction**: A (local Canvas crop + aspect presets + output width/height; do not split near-duplicate URLs for `crop-image` / `resize-image` / platform-only pages)
- **status**: `02` = `ready`; `03` = `briefs-ready` (ten-locale outcome-oriented Titles; no parameter-list catalog tone)
- **boundaries**: distinct from `image-compress` (target size), `image-format-converter` (MIME), and direction B platform compliance packs; v1 should not upscale pixels without purpose
- **implementation**: not started; wait for an explicit “implement” request, then follow `tool-creation.mdc`

[actions]
- Added/completed `work-tasks/image-crop/00-request.md`, `01-direction-discussion.md`, `02-tool-info.md`, `03-locale-briefs.md`
