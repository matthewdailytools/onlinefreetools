Date: 2026-08-07 14:31
Summary: Completed image-crop tool brief (work-tasks) including ten-locale briefs.

[question]
立项：图片尺寸裁剪

[try to solve]
立项完成，目录 `work-tasks/image-crop/`：

- **slug / 路径**：`image-crop` → `/tools/image-crop`（分类 `image`）
- **方向**：A（Canvas 本地裁剪 + 比例预设 + 输出宽高；不拆 `crop-image` / `resize-image` / 平台专用近义 URL）
- **状态**：`02` = `ready`；`03` = `briefs-ready`（十语 Title 结果向，禁参数枚举目录腔）
- **边界**：与 `image-compress`（目标体积）、`image-format-converter`（格式）、方向 B 平台合规包区分；v1 建议不无意义放大像素
- **实现**：未开始；需明确「实现」后再按 `tool-creation.mdc` 落地

[actions]
- Added/completed `work-tasks/image-crop/00-request.md`, `01-direction-discussion.md`, `02-tool-info.md`, `03-locale-briefs.md`
