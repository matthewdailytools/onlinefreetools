# Date: 2026-08-07 15:38
Summary: Brief photo smart compress maps to existing image-compress; no new slug.

[question]
立项：照片图片智能压缩

[try to solve]
「照片图片智能压缩」与已上线的 `image-compress`（方向 A.2「智能压缩」）为同一任务与同一流水线，**不单独新立项、不注册近义 URL**。

已落盘：`work-tasks/photo-smart-compress/`（`00`/`01`/`02`，状态 `wont-create`，映射 `/tools/image-compress`）。「智能」= 目标体积逼近策略（方向表对 `browser-image-compression` 的别称）；「照片」= 输入场景，不拆页。若要增强既有压缩页（库切换、批量、预设），需另开增强需求。

[actions]
- 新增 `work-tasks/photo-smart-compress/00-request.md`
- 新增 `work-tasks/photo-smart-compress/01-direction-discussion.md`
- 新增 `work-tasks/photo-smart-compress/02-tool-info.md`（`wont-create`）
