Date: 2026-08-07 15:38
Summary: Mapped “photo smart compress” to existing image-compress; no new slug.

[question]
立项：照片图片智能压缩

[try to solve]
“Photo smart compress” is the same job and pipeline as the already-shipped `image-compress` (direction A.2 “smart compress”). Do not open a separate brief or register a near-duplicate URL.

Filed under `work-tasks/photo-smart-compress/` (`00`/`01`/`02`, status `wont-create`, maps to `/tools/image-compress`). “Smart” = target-size approximation strategy (direction-table alias for `browser-image-compression`); “photo” = input scene, not a separate page. Enhancements to the existing compress page (library swap, batch, presets) need a separate enhancement request.

[actions]
- Added `work-tasks/photo-smart-compress/00-request.md`
- Added `work-tasks/photo-smart-compress/01-direction-discussion.md`
- Added `work-tasks/photo-smart-compress/02-tool-info.md` (`wont-create`)
