Date: 2026-08-13 00:22
Summary: Diagnose upload:r2 403 Forbidden as R2 REST auth/account/bucket issue.

[question]
npm run upload:r2 → wrangler put 403 Forbidden on `_pages/ar/privacy.html.gz`

[try to solve]
这是 Cloudflare REST 侧拒绝写 R2，不是脚本逻辑错误。本地 `upload:r2:local` 不受影响。请在本机跑 `wrangler whoami` / `r2 bucket list`，确认桶在正确账户且已创建；多账户时 logout+login 并清 `wrangler-account.json`；若用 `CLOUDFLARE_API_TOKEN`，Object Read/Write 对 wrangler put 不够，需 Admin Read & Write 或改用 S3 API。手册已加 §9.1；upload 脚本在 403 时给出同向提示。

[actions]
- Updated `ops/worker-r2-ops.md` §9.1
- Updated `scripts/upload-r2-pages.mjs` 403 hint
