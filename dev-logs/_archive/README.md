# Archived dev logs (not published)

Logs moved here fail the relevance screen in `.cursor/rules/dev-logs.mdc`
(mostly pure `git add` / `commit` / `push` sessions, or meta-log noise).

- **Not** scanned by `buildDevLogs` (directories starting with `_` are skipped).
- Kept for local audit only; do not rewrite into thin public pages.
- Manifest: `dev-logs/_curation/2026-08-26-archive-manifest.json`
