# Archived dev logs (not published)

Logs moved here were judged **not worth keeping on the public site at all**
under `.cursor/rules/dev-logs.mdc` (mostly pure `git add` / `commit` / `push`
sessions, empty confirmations, or meta-log noise).

Project-needed but not people-helpful posts should stay under `dev-logs/YYYY-MM/`
with `Visibility: project` (noindex, nofollow; omitted from sitemap)—do **not**
archive those unless the user asks to remove them from the public tree.

- **Not** scanned by `buildDevLogs` (directories starting with `_` are skipped).
- Kept for local audit only; do not rewrite into thin public pages.
- Manifest: `dev-logs/_curation/2026-08-26-archive-manifest.json`
