# 01 — 方向讨论

## 结论

主方向 **A**：浏览器 JS。hub **L2** scene：多文件统一裁掉相同片头秒数，导出裁剪后 WAV 的 ZIP。

## 队列位置

A8 / A6 之后；本批为 **L2**。

## 技术取舍

- 本地处理，不上传。
- Rich ten locales；诚实边界写入 FAQ / Rules。
- ≠ 相邻工具职责（见 02 同意图表）。

## 下一步

02/03 → 实现 → `CROSS_TOOL_UPDATE=1 npm run verify:tool -- --slug=batch-trim-the-same-intro-from-audio-files`。
