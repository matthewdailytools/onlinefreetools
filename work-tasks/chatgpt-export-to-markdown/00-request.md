# 00 — 用户原始需求

> 只记录用户原话与已知约束；讨论与定稿写在 `01` / `02`。

## 原始描述

继续

（P1 队列下一条：`chatgpt-export-to-markdown`。）

## 已知约束（若有）

- 参考现有工具：`yaml-json` / `csv-json`（本地文本互转）；旧名 `chat-export-converter` **勿注册**
- 必须本地处理：是
- YMYL：否
- 优先语言：en 母版，十语
- 其他：清单 §0 P1；H1 = **ChatGPT export to Markdown**；默认导出 → Markdown；JSON / CSV 为输出芯片；Claude 导出 absorb（同页，不拆 URL）；无 Planner 批；`ai-token-counter` 为 P2 defer，related 勿依赖未上线 slug

## 建议 slug

- `chatgpt-export-to-markdown`
