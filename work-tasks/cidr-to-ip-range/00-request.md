# 00 — 用户原始需求

> 只记录用户原话与已知约束；讨论与定稿写在 `01` / `02`。

## 原始描述

立项：cidr notation calculator

## 已知约束（若有）

- 参考现有工具：同簇已有 `ip-range-to-cidr`、`terraform-cidrsubnet`、`private-cidr-ranges`；Planner 收割页映射见 `docs/seo/keywords/cidr/2026-08-28-cidr-keyword-planner.md`
- 必须本地处理：是
- YMYL：否
- 优先语言：en 母版 + 十语 brief
- 其他：用户点名头词邻接说法「cidr notation calculator」；**禁止**以 `CIDR Calculator` / `CIDR Notation Calculator` 作唯一 H1；须按 title_gap_fallback 立项为 `cidr-to-ip-range`

## 建议 slug（可空，由讨论阶段拟定）

- `cidr-to-ip-range`（Planner 选用；H1 = CIDR to IP range；`cidr notation calculator` absorb）
