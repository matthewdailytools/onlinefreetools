# 00 — 用户原始需求

> 只记录用户原话与已知约束；讨论与定稿写在 `01` / `02`。

## 原始描述

继续，直到最后一个slug

（P1 队列：`curl-to-fetch`。）

## 已知约束（若有）

- 参考现有工具：`jwt-decoder` / `website-headers`（本地文本解析）
- 必须本地处理：是
- YMYL：否
- 优先语言：en 母版，十语
- 其他：清单 §0 P1；H1 = **curl to fetch**；把 cURL 命令转成 JavaScript `fetch`；无 Planner 批；不拆 axios/Python 独立 URL（可 FAQ absorb）

## 建议 slug

- `curl-to-fetch`
