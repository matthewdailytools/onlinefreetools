# 00 — 用户原始需求

> 只记录用户原话与已知约束；讨论与定稿写在 `01` / `02`。

## 原始描述

立项：HTML 实体编解码

## 已知约束（若有）

- 参考现有工具：`markdown-to-html`、`text-diff`、`yaml-json`（开发者/文本格式链）
- 必须本地处理：是（粘贴/转换不上传；若用 CDN 载库须在 FAQ 诚实说明）
- YMYL：否
- 优先语言：十语（en 母版 → 按 `03-locale-briefs.md` 逐语重写）
- 其他：方向文档 A.1 已列「HTML 实体编解码 · `he` 或原生 · Tier 0」

## 建议 slug（可空，由讨论阶段拟定）

- `html-entity`（路径 `/tools/html-entity`；编解码同页 Tab，不拆 `html-entity-encoder` / `html-entity-decoder` 近义薄页）
