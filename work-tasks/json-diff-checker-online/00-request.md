# 00 — 用户原始需求

## 原始描述

text-compare topic：两份 JSON 看语义差。H1 = **JSON diff checker online**（禁 H1=`JSON Diff Checker` 头词）。默认忽略键序；路径级增删改。

## 已知约束（若有）

- `JSON.parse`；不解析 YAML/XML
- 必须本地处理：是
- YMYL：否
- 优先语言：十语
- Related：`json-schema-validator`、`yaml-json`

## 建议 slug

- `json-diff-checker-online`
