# 00 — 用户原始需求

> 只记录用户原话与已知约束；讨论与定稿写在 `01` / `02`。

## 原始描述

立项：remove-silence-from-a-recording，特别注重要seo优化必须要优秀

## 已知约束（若有）

- 参考现有工具：`docs/sound-editor/12-slug-hub-and-scene.md` **S2**（scene：长录音里挖掉静音，**仍一个文件**；近义不拆 `/delete-silence`；与 **A3** 按静音切 ZIP 不同作业）；已上线对照 `trim-an-audio-clip-and-export`（S1 起止裁剪，不是自动挖中间静音）
- 必须本地处理：是
- YMYL：否
- 优先语言：en 母版；十语 brief 按当地搜法重跑（用户点名 SEO 必须优秀）
- 其他：用户点名 slug；**本回合立项**（`02` ready + 0b/0i），不实现 `src/` 页面。仓库内无本 slug 的 Keyword Planner CSV（12 表写明声音簇未跑 Planner）。Ahrefs MCP 本回合不可用，覆盖表用公开 SERP / 竞品 related / PAA / 当地搜法。

## 建议 slug（可空，由讨论阶段拟定）

- `remove-silence-from-a-recording`（用户指定；场景句：从一段录音去掉静音，结果仍是一个文件）
