# 00 — 用户原始需求

> 只记录用户原话与已知约束；讨论与定稿写在 `01` / `02`。

## 原始描述

立项和实现：split-a-disc-image-with-a-cue-sheet

## 已知约束（若有）

- 参考现有工具：`docs/sound-editor/12-slug-hub-and-scene.md` **S4**（scene：按 cue / 章节表分轨）；已上线邻作业 S3 `split-an-audio-file-by-duration`（按时长切 ZIP）、A3 `split-a-recording-on-silence`（按静音切 ZIP）、S1 `trim-an-audio-clip-and-export`、S2 `remove-silence-from-a-recording`
- 必须本地处理：是
- YMYL：否
- 优先语言：十语（母版 en）
- 其他：用户点名 12 表草稿 slug；近义不拆各容器 URL（ape/flac/wav/bin）；不是静音切、不是等时长切、不是 Hub 波形精剪；不是 YouTube 章节手标切点页

## 建议 slug（可空，由讨论阶段拟定）

- `split-a-disc-image-with-a-cue-sheet`（用户指定；§3.3 H 情境=整轨镜像 + 动作=按 cue 表切开 + 结果=分轨文件）
