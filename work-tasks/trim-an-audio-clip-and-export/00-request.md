# 00 — 用户原始需求

> 只记录用户原话与已知约束；讨论与定稿写在 `01` / `02`。

## 原始描述

立项：trim-an-audio-clip-and-export，归属新的topic:sound editor

## 已知约束（若有）

- 参考现有工具：`docs/sound-editor/12-slug-hub-and-scene.md` **S1**（hub 且 scene：进页=起止+导出，不是缩小 DAW）；`docs/sound-editor/11-executable-plan.md`（Hub 是另一 slug，未点名不建）
- 必须本地处理：是
- YMYL：否
- 优先语言：en 母版；十语 brief 按当地搜法
- 其他：用户点名 slug 与新主题 id；**本回合立项**（`02` ready），不实现 `src/` 页面

## 建议 slug（可空，由讨论阶段拟定）

- `trim-an-audio-clip-and-export`（用户指定；场景句：裁一段并导出）
