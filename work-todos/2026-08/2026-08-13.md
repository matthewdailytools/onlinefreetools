# 2026-08-13

## Focus
- Worker + R2 发版链路收尾（gitignore `_pages`、GitHub 部署）
- 增量 upload R2（减慢全量 put）

## Todo
- [ ] **增量 upload R2**：`scripts/upload-r2-pages.mjs` 按内容 hash / ETag 跳过未变 `.html.gz`，只 put 变更与新增；更新 `_meta/pages-build.json`；本地 `--local` 同逻辑；运维文档注明（见 `ops/worker-r2-ops.md`）
- [ ] repair-toolbox 方案细化（续 `2026-08-12`，见 `docs/repair-toolbox-方案文档.md`）

## Done
- Worker + R2 主链路落地（预渲染 → R2 → Cache；`.assetsignore`；deploy → git push）
- `public/_pages/` 整树 gitignore（索引已卸，待 commit/push）
- 首页性能：分类 `<details>` 默认只展开首类、content-visibility、首屏 12 图标 eager、去掉 CSS preload 双写、侧栏锚点自动展开
- 首页进 Static Assets（常规 URL）：`public/index.html` + `public/{lang}/index.html`；Worker `serveHomeHtml`；`run_worker_first` 含 `/` 与各语前缀

## Notes
- 当前 `upload:r2` 每次全量 ~1510 次 wrangler put，改/加几个工具也全量；增量优先于 S3 批量，也可后续再换 S3 API
- 昨日「框架改为 Worker + R2」可视为工程主路径已完成；剩余是发版体验（增量上传）与文档/入库清理
